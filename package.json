{
	"name": "bird-shortcuts",
	"version": "0.1.0",
	"devDependencies": {
		"@biomejs/biome": "^2.2.2",
		"@types/thunderbird-webext-browser": "^127.0.0",
		"dotenv-cli": "^10.0.0",
		"tsup": "^8.5.0",
		"typescript": "^5.9.2",
		"web-ext": "^8.9.0"
	},
	"scripts": {
		"build:bg": "tsup src/background.ts --format iife --global-name=bg --minify --sourcemap --out-dir dist",
		"build:exp": "tsc -p tsconfig.experiments.json && cp experiments/schema.json dist/experiments/schema.json",
		"build:static": "cp -r icons dist/icons && cp src/manifest.json dist/manifest.json",
		"build": "pnpm run build:bg && pnpm run build:exp && pnpm run build:static",
    "dev": "pnpm run build && dotenv -e .env.tb -- node scripts/run-webext.mjs",
		"lint": "biome lint .",
		"format": "biome format . --write"
	}
}
